version: '3'

services:
  backend:
    build: ./backend
    ports:
      - "3001:3001"
    volumes:
      - ~/.aws:/root/.aws:ro

  frontend:
    build: ./frontend
    ports:
      - "5173:80"
    depends_on:
      - backend
